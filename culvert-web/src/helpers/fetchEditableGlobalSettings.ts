const fetchEditableGlobalSettings = async (
	auth: string,
	// ): Promise<any> => {
): Promise<any> => {
	// TODO: YEET THE ANY AWAY
	// try {
	// 	const res = await fetch("/api/maple/characters/fetch", {
	// 		headers: {
	// 			Authorization: `Bearer ${auth}`,
	// 		},
	// 	});
	// 	if (res.status !== 200) {
	// 		return Promise.resolve(res.status);
	// 	}
	// 	return await res.json();
	// } catch (e) {
	// 	return Promise.resolve(-1);
	// }
};

export default fetchEditableGlobalSettings;
