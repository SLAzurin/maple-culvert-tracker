{
    "name": "validate-build-services",
    "private": true,
    "scripts": {
        "gen": "bun run src/generate-json-schema.ts",
        "build": "tsc && bun run gen && bun build --minify --target=node --outfile=./dist/index.js ./src/index.ts"
    },
    "devDependencies": {
        "@actions/core": "^2.0.2",
        "zod": "^4.3.6"
    }
}